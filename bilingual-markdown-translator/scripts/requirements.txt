# bilingual-markdown-translator 依赖

安装方式（任选其一）：

  # 方式一：运行 setup.py 自动创建独立环境（推荐）
  python setup.py

  # 方式二：使用已有 Python
  pip install -r requirements.txt

依赖清单：
requests>=2.28
html2text>=2024.0
beautifulsoup4>=4.12
cloudscraper>=1.2
